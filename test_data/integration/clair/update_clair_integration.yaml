space_name: $SPACE_NAME
name: $integration_name
description: $description
enabled: true
fields:
  endpoint: $CLAIR_ENDPOINT
  service_port: $CLAIR_SERVICE_PORT
  health_port: $CLAIR_HEALTH_PORT
  database_addr: $DATABASE_ADDR
  database_name: $DATABASE_NAME
  user_name: $DATABASE_USER_NAME
  password: $DATABASE_PASSWORD
namespace: $NAMESPACE